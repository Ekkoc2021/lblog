spring:
  ai:
    deepseek:
      api-key: ${AI_DEEPSEEK_API_KEY}
      chat:
        options:
          model: ${AI_DEEPSEEK_MODEL:deepseek-v4-flash}
          temperature: 0.7
          max-tokens: 16384
          extra-body:
            thinking:
              type: enabled
